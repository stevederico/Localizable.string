# Localizable.string

Multilingual localization strings library supporting 16+ languages for iOS apps.

## Supported Languages

Arabic, Chinese, Dutch, English, French, German, Italian, Japanese, Korean, Portuguese, Russian, Spanish, Thai, Turkish, Vietnamese

## Structure

Each language has its own `.lproj` directory containing `Localizable.strings` files with translated key-value pairs for common iOS app UI strings.
